{
  "cmd": "[ -r '${project_path:${folder}}/config.rb' ] && compass compile '${project_path:${folder}}' --boring; [ -r '${file_path}/config.rb' ] && compass compile '${file_path}' --boring; [ -r '${file_path}/../config.rb' ] && compass compile '${file_path}/../' --boring",
	"working_dir": "$packages/CompassBuild",
	"selector": "source.sass, source.scss",
	"shell": "true",
}
